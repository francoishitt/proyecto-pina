"use client";
import {useForm} from "react-hook-form"; import {zodResolver} from "@hookform/resolvers/zod"; import {toast} from "sonner"; import {useEffect,useState} from "react"; import {Loader2} from "lucide-react"; import {useRouter} from "next/navigation"; import {categoriaSchema,CategoriaFormData} from "@/lib/validations/categoria.schema"; import {crearCategoria,actualizarCategoria} from "@/actions/categoria.action"; import type {CategoriaConRelaciones} from "../types";
interface Props{modo:"crear"|"editar";categoria:CategoriaConRelaciones|null;onGuardado:()=>void;onCancelar:()=>void}
const slug=(s:string)=>s.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/\s+/g,"-").replace(/[^a-z0-9-]/g,"");
export default function Formulario({modo,categoria,onGuardado,onCancelar}:Props){const[busy,setBusy]=useState(false);const router=useRouter();const{register,handleSubmit,formState:{errors},setValue,reset,watch}=useForm<CategoriaFormData>({resolver:zodResolver(categoriaSchema),defaultValues:{nombre:categoria?.nombre||"",slug:categoria?.slug||"",descripcion:categoria?.descripcion||"",orden:categoria?.orden??0,visible:categoria?.visible??true}});useEffect(()=>reset({nombre:categoria?.nombre||"",slug:categoria?.slug||"",descripcion:categoria?.descripcion||"",orden:categoria?.orden??0,visible:categoria?.visible??true}),[categoria,reset]);const visible=watch("visible");
const submit=async(d:CategoriaFormData)=>{setBusy(true);const fd=new FormData();fd.append("nombre",d.nombre);fd.append("slug",d.slug);fd.append("descripcion",d.descripcion||"");fd.append("orden",String(d.orden));fd.append("visible",String(d.visible));try{const r=modo==="crear"?await crearCategoria(fd):await actualizarCategoria(categoria!.id,fd);if(r.success){toast.success("Cambios guardados correctamente");router.refresh();onGuardado()}else toast.error(r.error||"No se pudo guardar")}finally{setBusy(false)}};
return <form onSubmit={handleSubmit(submit)} className="bg-white rounded-xl border border-slate-300 p-6 space-y-6"><h2 className="text-xl font-semibold">{modo==="crear"?"Nueva categoría":"Editar categoría"}</h2><div className="grid md:grid-cols-2 gap-5"><label className="text-sm font-semibold">Nombre<input {...register("nombre")} onChange={e=>{register("nombre").onChange(e);setValue("slug",slug(e.target.value))}} className="mt-1 w-full border rounded-lg px-3 py-2"/>{errors.nombre&&<small className="text-red-500">{errors.nombre.message}</small>}</label><label className="text-sm font-semibold">Slug<input {...register("slug")} className="mt-1 w-full border rounded-lg px-3 py-2"/></label><label className="text-sm font-semibold">Orden en la cinta<input type="number" min="0" {...register("orden")} className="mt-1 w-full border rounded-lg px-3 py-2"/><small className="text-slate-400">0 aparece primero.</small></label><label className="text-sm font-semibold flex items-center gap-3 mt-6"><input type="checkbox" checked={visible} onChange={e=>setValue("visible",e.target.checked)} className="w-5 h-5"/>Visible en la cinta pública</label><label className="md:col-span-2 text-sm font-semibold">Descripción<textarea {...register("descripcion")} rows={3} className="mt-1 w-full border rounded-lg px-3 py-2"/></label></div><div className="flex justify-end gap-3 border-t pt-4"><button type="button" onClick={onCancelar} className="px-5 py-2.5 border rounded-lg">Cancelar</button><button disabled={busy} className="px-5 py-2.5 bg-orange-600 text-white rounded-lg font-semibold flex gap-2 items-center">{busy&&<Loader2 size={16} className="animate-spin"/>}{modo==="crear"?"Crear categoría":"Guardar cambios"}</button></div></form>}
