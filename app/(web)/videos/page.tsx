import {obtenerVideosSociales} from "@/lib/social";import VideosSociales from "@/components/VideosSociales";export const dynamic="force-dynamic";export default async function Page(){const videos=await obtenerVideosSociales(60);return <main className="bg-slate-50 min-h-screen"><div className="container mx-auto px-4 pt-12"><h1 className="text-3xl font-bold text-blue-950">Videos de Academias Proyecto Piña</h1><p className="text-slate-500 mb-2">TikTok e Instagram sincronizados automáticamente.</p></div>{videos.length?<VideosSociales videos={videos} titulo="Últimos videos" verTodos={false}/>:<div className="container mx-auto px-4 py-20 text-center text-slate-500">Todavía no hay una cuenta social conectada.</div>}</main>}