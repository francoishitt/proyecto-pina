import {NextResponse} from "next/server";import {cookies} from "next/headers";import crypto from "crypto";import {obtenerUsuarioSesion} from "@/lib/auth";export async function GET(){const u=await obtenerUsuarioSesion();const base=process.env.NEXT_PUBLIC_SITE_URL!;if(!u||!["ADMIN","SUPERVISOR"].includes(u.rol))return NextResponse.redirect(new URL("/login",base));const id=process.env.INSTAGRAM_APP_ID;if(!id)return NextResponse.redirect(new URL("/admin/videos?error=instagram_config",base));const state=crypto.randomBytes(20).toString("hex");(await cookies()).set("ig_oauth_state",state,{httpOnly:true,secure:true,sameSite:"lax",maxAge:600,path:"/"});const redirect=`${base}/api/social/instagram/callback`;const url=new URL("https://www.instagram.com/oauth/authorize");url.searchParams.set("client_id",id);url.searchParams.set("redirect_uri",redirect);url.searchParams.set("response_type","code");url.searchParams.set("scope","instagram_business_basic");url.searchParams.set("enable_fb_login","0");url.searchParams.set("force_authentication","1");url.searchParams.set("state",state);return NextResponse.redirect(url)}
