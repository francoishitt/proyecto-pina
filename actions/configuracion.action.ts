"use server";
import { prisma } from "@/lib/prisma"; import { exigirGestorEstructura,mensajeErrorPermisos } from "@/lib/auth"; import { revalidatePath } from "next/cache";
export const CONFIG_DEFAULT={id:"1",nombreCentro:"Proyecto Piña",tituloSitio:"Proyecto Piña | Academia Pre-Universitaria",descripcionSeo:"Preparación preuniversitaria en Iquitos",logoUrl:null,whatsapp:"51925030648",whatsappMensaje:"Hola Proyecto Piña, deseo solicitar información.",telefono:"+51 925 030 648",direccion:"Iquitos, Loreto, Perú",emailContacto:"informes@proyectopina.com",facebook:null,instagram:null,tiktok:null,youtube:null,heroTitulo:"Prepárate para ingresar",heroSubtitulo:"Material académico y preparación preuniversitaria",mostrarVideosInicio:true,cantidadVideosInicio:6};
export async function obtenerConfiguracion(){try{const data=await prisma.configuracionWeb.findUnique({where:{id:"1"}});return{success:true,data:data??CONFIG_DEFAULT}}catch(e){console.error(e);return{success:false,data:CONFIG_DEFAULT,error:"No se pudo cargar la configuración."}}}
export async function guardarConfiguracion(datos:Record<string,unknown>){try{await exigirGestorEstructura();const clean={nombreCentro:String(datos.nombreCentro||"Proyecto Piña").trim(),tituloSitio:String(datos.tituloSitio||"Proyecto Piña").trim(),descripcionSeo:String(datos.descripcionSeo||"").trim()||null,logoUrl:null,whatsapp:String(datos.whatsapp||"").replace(/\D/g,""),whatsappMensaje:String(datos.whatsappMensaje||"").trim()||null,telefono:String(datos.telefono||"").trim()||null,direccion:String(datos.direccion||"").trim()||null,emailContacto:String(datos.emailContacto||"").trim(),facebook:String(datos.facebook||"").trim()||null,instagram:String(datos.instagram||"").trim()||null,tiktok:String(datos.tiktok||"").trim()||null,youtube:String(datos.youtube||"").trim()||null,heroTitulo:String(datos.heroTitulo||"Prepárate para ingresar").trim(),heroSubtitulo:String(datos.heroSubtitulo||"Material académico y preparación preuniversitaria").trim(),mostrarVideosInicio:Boolean(datos.mostrarVideosInicio),cantidadVideosInicio:Math.max(1,Math.min(12,Number(datos.cantidadVideosInicio)||6))};const data=await prisma.configuracionWeb.upsert({where:{id:"1"},update:clean,create:{id:"1",...clean}});revalidatePath("/","layout");revalidatePath("/contacto");revalidatePath("/cursos");revalidatePath("/videos");return{success:true,data}}catch(e){const p=mensajeErrorPermisos(e);if(p)return{success:false,error:p};console.error(e);return{success:false,error:"No se pudo guardar la configuración."}}}
